PORT=8787
LLM_API_KEY=

# Future — Postgres on Railway (Drizzle). Do not use until the DB task.
# DATABASE_URL=

# Future — Better Auth. Do not use until the auth task.
# BETTER_AUTH_SECRET=
# BETTER_AUTH_URL=
