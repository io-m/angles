import SwiftUI

@main
struct AnglesApp: App {
    var body: some Scene {
        WindowGroup {
            // Screens/UI will be specified separately. Do not add feature views here.
            // TODO(persistence): History and saved reframes need local storage
            // (SwiftData or similar). Do not add until that task is specified.
            Text("Angles")
        }
    }
}
