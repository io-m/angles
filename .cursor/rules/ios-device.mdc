---
description: Always run and install Angles on the physical Joe’s iPhone — never stop at Simulator
alwaysApply: true
---

# Deploy target

After any iOS UI or app change, **install and launch on Joe’s iPhone**. A Simulator compile is not done.

- Device: **Joe’s iPhone** (iPhone 14 Pro Max)
- Destination id: `E5C20243-B9B7-571E-9EEA-14FC441C13B7`
- Bundle id: `app.angles.ios`
- Do **not** use Simulator (`generic/platform=iOS Simulator`, `iphonesimulator`) as the verification target.
- Do **not** tell the user the work is visible until `devicectl` install + launch has succeeded.

```bash
cd AnglesApp
xcodebuild -project AnglesApp.xcodeproj -scheme AnglesApp -configuration Debug \
  -destination 'id=E5C20243-B9B7-571E-9EEA-14FC441C13B7' \
  -derivedDataPath DerivedData -allowProvisioningUpdates build
xcrun devicectl device install app --device E5C20243-B9B7-571E-9EEA-14FC441C13B7 \
  DerivedData/Build/Products/Debug-iphoneos/Angles.app
xcrun devicectl device process launch --device E5C20243-B9B7-571E-9EEA-14FC441C13B7 \
  app.angles.ios
```
